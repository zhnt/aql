// 调试数组访问问题

// 测试一维数组访问
let arr1d = [1, 2, 3];
let element1d = arr1d[0];  // 应该是 1

// 测试二维数组第一层访问
let matrix = [[1, 2], [3, 4]];
let row = matrix[0];       // 应该是 [1, 2]

// 直接返回简单的数字，避免ToString问题
return 42; 