// 二维数组测试 - 简化版本

// 创建一个简单的二维数组
let matrix = [
    ["a", "b", "c"],
    ["d", "e", "f"],
    ["g", "h", "i"]
];

// 访问二维数组的第一层（获取子数组）
let firstRow = matrix[0];
let secondRow = matrix[1];
let thirdRow = matrix[2];

// 创建数字矩阵
let numMatrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

// 访问数字矩阵的第一层
let numFirstRow = numMatrix[0];
let numSecondRow = numMatrix[1];

// 修改矩阵的第一层元素
matrix[0] = ["X", "Y", "Z"];

// 简单的计算验证
let result = 1 + 2;
return result; 