// AQL数组基础功能测试

// 1. 数组创建和初始化
let arr1 = [1, 2, 3, 4, 5];
let arr3 = [10];

// 2. 数组访问
let element1 = arr1[0];  // 1
let element2 = arr1[2];  // 3
let element3 = arr1[4];  // 5

// 3. 字符串数组
let strings = ["hello", "world", "test"];
let str1 = strings[0];
let str2 = strings[2];

// 4. 混合类型数组
let mixed = [1, "text", 3, "end"];
let num = mixed[0];
let text = mixed[1];

// 5. 单元素数组
let single = arr3[0];  // 10

// 返回验证结果
element1 + element2 + element3 + single;  // 1 + 3 + 5 + 10 = 19 