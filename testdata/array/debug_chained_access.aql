// 调试连续数组访问问题

// 创建二维数组
let matrix = [[1, 2], [3, 4]];

// 分步访问
let row = matrix[0];        // 第一步：获取第一行 [1, 2]
let element = row[0];       // 第二步：获取第一个元素 1

// 连续访问
let directElement = matrix[0][0];  // 直接访问：应该是 1

// 返回这三个值的和，应该是 1 + 1 + 1 = 3
return element + directElement + 1; 